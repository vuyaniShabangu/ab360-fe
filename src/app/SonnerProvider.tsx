'use client'

import { Toaster } from '@/components/ui/sonner';

const SonnerProvider = () => {

  return (<Toaster richColors expand={true} position="top-center"/>)
}

export default SonnerProvider;
